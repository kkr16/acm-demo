apiVersion: v1
kind: ConfigMap
metadata:
  name: helm-apache-values-2
  namespace: config-management-system
  annotations:
    configmanagement.gke.io/cluster-selector: selector-env-dev
immutable: true
data:
  values.yaml: |-
    replicaCount: 1